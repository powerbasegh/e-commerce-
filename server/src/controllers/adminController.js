const db = require('../config/db');
exports.listOrders = async (req,res)=>{ const [rows]=await db.execute(`SELECT o.id,o.order_number,o.customer_name,o.customer_phone,o.subtotal,o.delivery_fee,o.grand_total,o.status,o.created_at,d.city,d.area,d.landmark,d.latitude,d.longitude,d.status delivery_status FROM orders o JOIN delivery_quotes d ON d.order_id=o.id WHERE d.status='PENDING' ORDER BY o.created_at DESC`); res.json({orders:rows}); };
exports.updateDeliveryFee = async (req,res)=>{ const orderId=Number(req.params.orderId); const rawFee=req.body.delivery_fee; const fee=Number(rawFee); const status=req.body.quote_status||'SET'; if(!Number.isInteger(orderId)||!['SET','WAIVED','CANCELLED'].includes(status)) return res.status(400).json({message:'Invalid order or quote status'}); if(status==='SET' && (!Number.isFinite(fee)||fee<0)) return res.status(400).json({message:'A valid non-negative delivery fee is required when setting a quote'}); const conn=await db.getConnection(); try{await conn.beginTransaction(); const [orders]=await conn.execute('SELECT id,user_id,subtotal,platform_fee,status FROM orders WHERE id=? FOR UPDATE',[orderId]); if(!orders.length)return res.status(404).json({message:'Order not found'}); const order=orders[0]; const actualFee=status==='WAIVED'?0:(status==='CANCELLED'?null:fee); const newStatus=status==='SET'?'DELIVERY_FEE_QUOTED':status==='WAIVED'?'DELIVERY_FEE_QUOTED':'CANCELLED'; const grand=actualFee == null ? Number(order.subtotal)+Number(order.platform_fee) : Number(order.subtotal)+Number(order.platform_fee)+actualFee; await conn.execute('UPDATE delivery_quotes SET delivery_fee=?,status=?,quoted_at=NOW(),quoted_by=? WHERE order_id=?',[actualFee,status,req.user.id,orderId]); await conn.execute('UPDATE orders SET delivery_fee=?,grand_total=?,status=? WHERE id=?',[actualFee,grand,newStatus,orderId]); const feeMessage = actualFee == null
  ? 'Your delivery fee quote was cancelled. PowerBase will contact you with the next steps.'
  : `Your delivery fee is GHS ${actualFee.toFixed(2)}.`;
await conn.execute('INSERT INTO order_events (order_id,status,title,description) VALUES (?,?,?,?)',[orderId,newStatus,'Delivery Fee Updated',feeMessage]);
await conn.execute('INSERT INTO notifications (user_id,type,title,message) VALUES (?,?,?,?)',[order.user_id,'DELIVERY_FEE_UPDATE',`Order #${orderId} delivery fee`,feeMessage]); await conn.commit(); res.json({order:{id:orderId,deliveryFee:actualFee,grandTotal:grand,status:newStatus}});}catch(e){await conn.rollback();console.error(e);res.status(500).json({message:'Failed to update delivery fee'});}finally{conn.release();}};
